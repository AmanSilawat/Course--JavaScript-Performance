# will-change

Create a own layer.
It's a documented, supported.
```css
.sidebar {
    will-change: transform;
}
```

<br />

**hack** force to make own layer
```css
.sidebar {
    transform: translateZ(0)
}
```
